import { ObjectSearchBar } from "@/components/bars";
import { ObjectTable } from "@/components/tables";

const Home = () => {
  return (
    <div>
      <ObjectSearchBar />
      <ObjectTable />
    </div>
  );
};

export default Home;
